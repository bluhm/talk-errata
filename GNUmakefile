NAME =	errata-slides

all:
	pdflatex $(NAME)
	pdflatex $(NAME)

clean:
	rm -f $(NAME).aux $(NAME).log $(NAME).nav $(NAME).out $(NAME).pdf \
	    $(NAME).snm $(NAME).toc $(NAME).vrb

.PHONY: all clean
